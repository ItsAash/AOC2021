#include<string>
#include<vector>

/**
 * @brief Split the string by any character.
 * 
 * @return std::vector<string> Vector with the string splited.
 */
std::vector<std::string> split(std::string, char);